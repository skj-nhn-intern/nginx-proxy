# Backend upstream — generated by deploy/apply-env-and-restart.sh from BACKEND_UPSTREAM
# 형식: BACKEND_UPSTREAM="ip_or_host:port" (예: 10.0.0.1:8000 또는 lb.example.com:80)
upstream photo_api_backend {
    server __BACKEND_UPSTREAM__;
    keepalive 32;
}
