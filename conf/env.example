# nginx-proxy NHN Deploy / 로컬 설정
# 복사 후 .env 로 저장하거나 NHN Deploy 환경 변수로 주입
# .env는 저장소에 올리지 말 것 (보안)

# 백엔드 업스트림 (필수). 추후 백엔드 API 로드밸런서 IP:포트로 변경
# 예: 10.0.0.1:8000, lb-internal.example.com:80
BACKEND_UPSTREAM="127.0.0.1:8000"

# LB 사설 IP — real_ip 신뢰용 (X-Forwarded-For 적용). 보안상 .env에만 설정, 저장소에 커밋 금지
# 예: REAL_IP_FROM="192.168.1.59" 또는 CIDR "192.168.1.0/24"
# REAL_IP_FROM=""

# server_name (선택). SSL 적용 시 도메인 지정에 사용
# SERVER_NAME="api.example.com"

# --- Observability (Promtail → Loki, Pushgateway) ---
# 로그·메트릭에 무조건 region 레이블 부여 (배포 시 설정)
# REGION="kr2"
# Loki: Promtail가 nginx access/error 로그 전송
# LOKI_URL="http://loki:3100"
# Pushgateway: node_exporter + nginx_exporter 메트릭 주기 푸시
# PROMETHEUS_PUSHGATEWAY_URL="http://pushgateway:9091"
# 인스턴스 식별 (미설정 시 hostname -I 첫 주소 사용)
# INSTANCE_IP=""
# PUSHGATEWAY_JOB="nginx-proxy"
